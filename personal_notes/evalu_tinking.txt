For Interview Preparation
"Tell me about this project":
"I built a RAG system for analyzing multiple academic papers simultaneously. The interesting challenge was that my initial advanced architecture—hybrid retrieval with reranking—actually performed worse than expected on multi-document queries.
Through systematic evaluation with RAGAS metrics, I identified 'retrieval starvation' as the core issue: the system wasn't retrieving enough relevant chunks from different papers. I solved this by tuning the ensemble weights to favor BM25 more heavily and increasing retrieval depth.
The optimizations improved context recall by 53% and faithfulness by 39%. What I learned is that architectural sophistication needs empirical validation—I wouldn't have caught this without rigorous metrics and A/B testing against a baseline."
"What was your biggest technical challenge?":
"Multi-document retrieval starvation. Initially, my context recall was only 0.49—the system was missing half the required information. This caused the LLM to hallucinate and fill gaps with pretrained knowledge, dropping faithfulness to 0.44.
I systematically tested three hypotheses:

Retrieval depth too shallow → Increased k from 10 to 25
Ensemble weights favoring semantic over keyword → Adjusted from 0.3/0.7 to 0.4/0.6
Prompt not constraining hallucination → Added strict citation requirements

The combination brought recall to 0.75 and faithfulness to 0.61. The key was having quantitative metrics to validate each change."
"Why didn't your advanced architecture beat the baseline on single documents?":
"Great question. On a single, well-structured paper like Vision Transformer, simple semantic search already retrieves the right chunks—there's no cross-document confusion to resolve. The reranker adds latency without adding value.
This taught me that architectural complexity should match task complexity. For production, I'd use semantic-only retrieval on single documents and hybrid retrieval for multi-document queries. That's a 3x latency improvement with equivalent accuracy."




HOW TO MAKE THIS BETTER
“I’d treat context recall as a retrieval-coverage problem first, not just a ranking problem. In this pipeline, I’d improve recall by making the retriever see more of the right evidence before reranking. Concretely, I’d:

use section-aware chunking with overlap so relevant passages are not split across boundaries,
preserve tables, figure captions, and headers as separate retrievable units,
widen the candidate set before reranking, for example retrieve more chunks and then rerank the best ones,
strengthen query reformulation so the system keeps technical entities, model names, datasets, and table-specific terms,
use a hybrid retrieval strategy more deliberately, tuning BM25 vs. dense retrieval weights for different query types,
and inspect failure cases with RAGAS to see whether misses come from chunking, embedding quality, query formulation, or reranking.”
Then I’d add:
“Once recall improves, I’d optimize precision with reranking and context compression so the final answer stays grounded and concise.”

Short version
If you want a crisp answer:

“I’d improve context recall by increasing retrieval coverage, not by only changing the LLM. I’d improve chunking and chunk overlap, preserve structured evidence like tables and figure captions, expand the candidate set before reranking, and use stronger query reformulation and hybrid retrieval. Then I’d evaluate failures with RAGAS to see exactly where the relevant context is being missed.”

What I’d mention as the most impactful first changes
Better chunking
Break documents into smaller, section-aware chunks
Add overlap between chunks
Keep table/figure context near the relevant text
Stronger retrieval coverage
Retrieve more candidates first
Then rerank aggressively
Query reformulation
Preserve names, tables, datasets, metrics
Expand vague follow-up questions using conversation history
Structured evidence handling
Treat tables and image captions as first-class retrieval units
Evaluate with RAGAS
Look at where recall drops
Identify whether failures are due to chunking, indexing, or query mismatch